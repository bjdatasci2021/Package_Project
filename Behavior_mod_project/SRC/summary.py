def summary(self, id, trigger):
        # based on emotion.id and emotion.mag 
        # return a random activity from a list that is the value in a dictionary (which has emotion.id as a key) in the file that the arbitor lives 
        # happy 5 will output value in a dictionary that dictionary 